<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.FLAG_IMMUTABLE"/>
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:networkSecurityConfig="@xml/network_security_config"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        tools:replace="android:icon"
        android:supportsRtl="true"
        android:theme="@style/Theme.SMDSForce"
        tools:targetApi="31">
        <activity
            android:name=".intent.Activity_RequestEdit"
            android:exported="false"
            android:label="@string/requestedit"/>
        <activity
            android:name=".intent.Activity_NewTrip"
            android:exported="false"
            android:theme="@style/AppThemeCustom"
            android:label="@string/newtrip"/>
        <activity
            android:name=".intent.Activity_ResetPassword"
            android:exported="false"
            android:label="@string/resetpassword" />
        <activity
            android:name=".intent.Activity_ChangePassword"
            android:exported="false"
            android:label="@string/changepassword" />
        <activity
            android:name=".intent.Activity_Settings"
            android:label="@string/setting" />
        <activity
            android:name=".intent.Activity_Validation"
            android:exported="false"
            android:theme="@style/AppThemeCustom" />
        <activity
            android:name=".intent.Activity_Trip"
            android:theme="@style/AppThemeCustom" />
        <activity
            android:name=".intent.Activity_Register"
            android:theme="@style/AppThemeCustom" />
        <activity
            android:name=".intent.Activity_RegForm"
            android:theme="@style/AppThemeCustom" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->


        <activity
            android:name=".intent.Activity_Maps"
            android:label="@string/title_activity__maps" />
        <activity
            android:name=".intent.Activity_Loading"
            android:theme="@style/AppThemeCustomLogin"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".intent.Activity_Main" />
        <activity
            android:name=".intent.Activity_Login"
            android:theme="@style/AppThemeCustomLogin" />
        <activity
            android:name=".intent.Activity_MainMenu"
            android:theme="@style/AppThemeCustom" /><!-- Sample AdMob App ID: ca-app-pub-3940256099942544~3347511713 -->
    </application>

</manifest>